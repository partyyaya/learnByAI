# 是否啟用前端 Mock（axios-mock-adapter）。
# true：所有 API 由 src/mock 攔截並回傳假資料（本專案預設）。
# false：走真實後端，請於 vite.config.js 設定 proxy。
VITE_USE_MOCK=true

# API base url。走 mock 時可留空；接真後端時填後端網址或代理前綴（例如 /api）。
VITE_API_BASE_URL=/api
